# Frontend Environment Configuration
# When deployed to Vercel, set VITE_API_BASE_URL in Vercel Project Settings > Environment Variables
# pointing to your deployed backend URL (e.g. Render, Railway, Fly.io, etc.)
# Leave empty for local development to use the Vite dev server proxy.
VITE_API_BASE_URL=
